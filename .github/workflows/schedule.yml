name: 🌤️ Weather Data Crawl

on:
  schedule:
    - cron: "0 * * * *"   # chạy mỗi 1 giờ (00:00, 02:00, 04:00, ...)
  workflow_dispatch:         # có thể chạy thủ công

jobs:
  crawl:
    runs-on: ubuntu-latest

    steps:
      # 🧩 1️⃣ Lấy code repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 🐍 2️⃣ Thiết lập Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # 📦 3️⃣ Cài đặt dependencies
      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      # ☁️ 4️⃣ Chạy script thu thập dữ liệu
      - name: Run weather crawler
        run: python weather_air_quality.py
        env:
          OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}

      # 💾 5️⃣ Commit và push dữ liệu mới trở lại repo
      - name: Commit and push updated CSV
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add weather_air_quality.csv
          git commit -m "Update weather data [skip ci]" || echo "No changes to commit"
          git push
